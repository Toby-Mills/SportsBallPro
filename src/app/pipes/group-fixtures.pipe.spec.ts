import { GroupFixturesPipe } from './group-fixtures.pipe';

describe('GroupFixturesPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupFixturesPipe();
    expect(pipe).toBeTruthy();
  });
});
