import { OpponentTeamPipe } from './opponent-team.pipe';

describe('OpponentTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new OpponentTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
