import { SortFixturesPipe } from './sort-fixtures.pipe';

describe('SortFixturesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFixturesPipe();
    expect(pipe).toBeTruthy();
  });
});
