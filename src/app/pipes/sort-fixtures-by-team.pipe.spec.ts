import { SortFixturesByTeamPipe } from './sort-fixtures-by-team.pipe';

describe('SortFixturesByTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFixturesByTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
